import 'package:flutter/material.dart';

class ContaUsPage extends StatelessWidget {
  const ContaUsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
